name: Ayu v0.3 PR Verify

on:
  pull_request:
    types: [opened]

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Verify pinned Telegram patch
        run: python3 ci_verify_v03.py
